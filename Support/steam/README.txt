To test the install script, you can try running this in an
administrator command prompt:

"C:\Program Files (x86)\Steam\bin\SteamService.exe" /installscript installscript_win.vdf 327140

This seems to do stuff, but I haven't actually gotten it to work.
